package org.shallow.internal.client;

import org.shallow.logging.InternalLogger;
import org.shallow.logging.InternalLoggerFactory;

public class Client2QuorumController {
    private static final InternalLogger logger = InternalLoggerFactory.getLogger(Client2QuorumController.class);

}
