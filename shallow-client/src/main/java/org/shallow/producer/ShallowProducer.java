package org.shallow.producer;

import org.shallow.logging.InternalLogger;
import org.shallow.logging.InternalLoggerFactory;

public class ShallowProducer {
    private static final InternalLogger logger = InternalLoggerFactory.getLogger(ShallowProducer.class);
}
