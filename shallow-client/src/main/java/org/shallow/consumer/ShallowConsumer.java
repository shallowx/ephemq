package org.shallow.consumer;

import org.shallow.logging.InternalLogger;
import org.shallow.logging.InternalLoggerFactory;

public class ShallowConsumer {
    private static final InternalLogger logger = InternalLoggerFactory.getLogger(ShallowConsumer.class);
}
